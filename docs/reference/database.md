# Database

The database layer is split into schema/bootstrap responsibilities and the raw
query helpers used throughout the UI and services.

## Schema

::: accessibility_mgr.db.schema

## Queries

::: accessibility_mgr.db.queries
